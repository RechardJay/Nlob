---
title: "令人迷惑的引用-指针永远不会安全"
date: "2025-11-20"
tags: "避坑"
---

Java发展自C++，没有了指针，取而代之的是引用。用官方的说法是：引用是安全的指针。

这一步好的很，把无数问题归为一类“空指针异常”NPE。

但 工具方法 `String.valueOf()` 有一个迷惑行为，直接传入null，会直接NPE。但传入一个指向null的String引用，他能正确返回字符串“null”。我们称这种方法是“空安全”，你传过来一个引用，我一定能返回对应的引用（返回至非空且非空引用）。